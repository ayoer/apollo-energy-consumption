services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apollo_frontend
    restart: unless-stopped
    ports:
      - '8080:80'

networks:
  default:
    name: apollo-network
    external: true
